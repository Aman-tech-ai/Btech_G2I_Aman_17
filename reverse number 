#include <stdio.h>

int main() {
    int n, r = 0, x = 0;

    printf("Enter the value of n : ");
    scanf("%d", &n);

    while (n > 0) {
        r = n % 10;
        x = x * 10 + r;
        n /= 10;
    }

    printf("%d", x);

    return 0;
}